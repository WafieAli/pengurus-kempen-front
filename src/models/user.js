export default class User {
  constructor(username, role, email, password, balance) {
    this.username = username;
    this.role = role;
    this.email = email;
    this.password = password;
    this.balance = balance;
  }
}
